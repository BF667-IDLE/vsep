# vsep - Core Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
requests>=2
numpy>=2
librosa>=0.10
samplerate==0.1.0
six>=1.16
torch>=2.3
tqdm
pydub>=0.25
onnx-weekly
onnx2torch-py313>=1.6
julius>=0.2
einops>=0.7
pyyaml
ml_collections
resampy>=0.4
beartype>=0.18.5
rotary-embedding-torch>=0.6.1
scipy>=1.13.0
soundfile>=0.12

# Platform-specific dependencies
diffq-fixed>=0.2; sys_platform == 'win32'
diffq>=0.2; sys_platform != 'win32'
audioop-lts>=0.2.1; python_version >= '3.13'

# Optional: GPU support (uncomment if needed)
# onnxruntime-gpu>=1.17

# Optional: CPU-only (default)
onnxruntime>=1.17

# Optional: DirectML for AMD/Intel GPUs (Windows)
# onnxruntime-directml>=1.17
# torch-directml
